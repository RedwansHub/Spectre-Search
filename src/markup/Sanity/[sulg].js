import React from 'react'

const JobDetails = ({ job, jobs}) => {
  const { image, name, details, price } = job;
  const [index, setIndex] = useState(0);
  
  return (
    <div>[sulg]</div>
  )}

export default sulg